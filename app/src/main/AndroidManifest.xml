<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.ck.ostapiuk.geekhubhomework">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AnimationsExamplesActivity"
            android:label="@string/title_activity_animations_examples" />
        <activity
            android:name=".activity.DocumentationViewerActivity"
            android:label="@string/title_activity_documentation_viewer"></activity>
    </application>

</manifest>
